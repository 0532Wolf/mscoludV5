server:
  port: 9528

spring:
  application:
    name: cloudalibaba-sentinel-gateway     # sentinel+gataway??Case
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    gateway:
      routes:
        - id: pay_routh1 #pay_routh1                #???ID(??mysql??ID)????????????????????
          uri: http://localhost:9001                #????????????
          predicates:
            - Path=/pay/**                      # ?????????????


